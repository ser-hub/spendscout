{% extends 'layout.html.twig' %}

{% block stylesheets %}
	<link href="{{ asset('styles/my-tags.css') }}" rel="stylesheet" data-turbo-track="reload">
{% endblock %}

{% block page_content %}
	<div class="layout-horizontal" data-controller="my-tags">
		<div class="tags-grid" data-my-tags-target="tagsGrid">
			{% for tag in user_tags %}
				<div class="tag-wrapper" data-id="{{ tag.id }}">
					{{ tag.name }}
                    <div>
					<i class="fa-solid fa-pencil fa-xl" data-action="click->my-tags#editMode"></i>
					<i class="fa-solid fa-trash fa-xl" data-action="click->my-tags#deleteTag"></i>
                    </div>
				</div>
			{% endfor %}
            {% if user_tags is empty %}
            No tags to show
            {% endif %}
		</div>
		<div class="tags-form">
			<div class="form-status" data-my-tags-target="tagsFormStatus">Create Tag</div>
			<div>
				<div class="form-error" data-my-tags-target="formError"></div>
				<input type="text" id="tag-name" name="_name" placeholder="Tag name" data-my-tags-target="tagsFormName">
			</div>
			<button type="submit" id="submit-btn" class="yellow-btn" data-action="my-tags#tagFormSubmit" data-my-tags-target="tagEditBtn">Save</button>
		</div>
	</div>
{% endblock %}
